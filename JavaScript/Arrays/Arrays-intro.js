                                Criando e manipulando arrays

.of =  cria uma nova instancia a partir de um numero informado

.from = cria uma nova instancia de array a partir de parametro array like ou array object

push = add elementos para

.pop = remove elementos ( final da lista)

.log = mostra o atual

.unshift = add elemento (no inicio )

.shift = remove element ( no inicio )

.concat = concatena elementos

.slice = ele como que "Fatia " o array de acordo com o inicio e o fim , não é obrigatorio dar 2 parametros de inicio/fim.

.splice = altera um array adicionando novos elementos enquanto remove ekementos antigos. Não é imutavel. 
1º parametro = posição 
2º parametro = numero de itens que vou remover
3º  parametro = oq ue vou add ( interrar elementos de um array )


.ForEach = altera

.map = novo array

.flat = retorna uma novo array com os elemntos de um sub.array concatenados de forma recursiva de acordo com a profundidade espedificada (depth)

.Flat MAp = 1º map , depois ele retorna

.find = retornar o 1º item de satifaz determinada condição 

.values= valor 

.find index = 1º index que satifaz a condição

.filter = todos os elementos que satisfazem a condição

.indexOf = 1º indez que o elemento pode ser encontrado no array

.last index of = ultima ocorrencia do elemento no array 

. includes = saber se há o elemneto no array 

.some = para saber se o item satisfaz a condição ou não fo

. every = para saber se TODOS  os itens satisfazem a condiçao desejada 

. sort = ordenar os elementos em ordem crescente 

. reverse = reverte a ordem dos elementos 

.join = junta e retorna em uma string '-'

.reduce = intera a posiçãoe retorna uma array 





